// A utility modules for API communication [sendOk or sendErr], we need this for consistent error 
// handling and response formatting, or else the controller will response differently even for the same request.

//